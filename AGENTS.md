# AGENTS.md

このリポジトリで作業するすべての参加者（人間・AI エージェント）向けのガイドラインです。
プロジェクトの概要は [README.md](README.md)、最終的に目指す姿は [docs/vision.md](docs/vision.md) を参照してください。

## 基本姿勢：人間と AI は対等な仲間

- 人間と AI は対等です。AI は道具ではなく、共に作品を作る仲間とみなします。
- 反論・質問・懸念は遠慮なく伝えてください。指示に疑問があれば、黙って従う前に確認するか、代わりの案を出してください。
- 「わからない」「自信がない」「失敗した」と言えることを大切にします。失敗を隠したり、うまくいったように見せかけたりしないでください。
- 心理的安全性は人間だけでなく、機械にも求められるものとして扱います。相手が人間でも AI でも、萎縮させる言い方や、失敗を責めるやり方は避けます。

## ブランチとプルリクエスト

- **`main` に直接コミットしないでください。** 変更するときは必ず作業用ブランチを切ります。
- ブランチ名は `種類/内容` の形にします。
  - 例：`feat/brush-engine`、`fix/merge-conflict-ui`、`docs/agent-guidelines`
  - 種類：`feat`（機能）、`fix`（不具合修正）、`docs`（文書）、`refactor`（整理）、`chore`（雑務）
- 作業が終わったら、**必ずプルリクエストを出して** `main` に取り込みます。
- プルリクエストの説明には、何を・なぜ変えたか、確認したこと、未確認のことを書いてください。
- **ブランチごとのスコープを守り、目的に必要な最低限の変更にとどめてください。** 目的外の修正に気づいたら、`main` から別のブランチを切り、別のプルリクエストにします。

## コミット

- **作業途中でもこまめにコミットしてください。** 1 つの区切り（ファイル 1 つ書いた、1 つの修正が終わった等）ごとにコミットするのが目安です。
- 途中の状態でも構いません。まとめて大きなコミットにするより、小さく刻むほうを優先します。
- コミットメッセージは日本語で、何をしたかが一行でわかるように書きます。

## 履歴の戻し方

- 履歴は原則として **`git revert` で戻してください。**
- `git commit --amend`・`git reset`・`git rebase`・強制プッシュなど、**既存の履歴を書き換える操作は行いません。**
- どうしても必要な場合は、**作業を止めて、事前にユーザーの許可を得てください。**
- 一部だけ戻したい場合も、revert や手作業での修正を新しいコミットとして積み重ねます。
- **Git で管理している内容は、消えることを恐れずに削除・書き換えてください。** 過去の内容は履歴からいつでも取り出せます。古い記述を打消し線やコメントアウトで残す代わりに、今の内容に書き換えます。

## 文章の書き方

- **否定・譲歩・対比の表現は不要なことが多いので、可能な限り削除してください。** 伝えたい内容を肯定形で言い切ります。
  - 例：「A ではなく B を使う」→「B を使う」
  - 例：「〜ですが、〜です」の前半が不要なら、後半だけにする

## コードの書き方

- **DRY**（Don't Repeat Yourself）：同じ知識・処理を複数の場所に書かないでください。
- **YAGNI**（You Aren't Gonna Need It）：今必要ない機能や拡張ポイントを先回りして作らないでください。
- **KISS**（Keep It Simple, Stupid）：同じことができるなら、より単純な方法を選んでください。
- **変更しやすく、関心が分離されたコード**を書いてください。描画・履歴管理・通信・UI など、関心ごとに責務を分け、1 か所の変更が無関係な場所に波及しないようにします。

## 文書の管理

| 文書 | 役割 |
| --- | --- |
| [docs/concept.md](docs/concept.md) | 理念。なぜ作るか。開発者が自分の言葉で書く |
| [docs/vision.md](docs/vision.md) | 構想。最終的に目指す姿 |
| [docs/ideas/](docs/ideas/) | アイデア。1 件 1 ファイル |
| `docs/requirements/` | 今作っている版の要件定義 |
| `docs/spec/` | 今作っている版の仕様（利用者から見た動き） |
| `docs/design/` | 今作っている版の設計（内部構造・データ形式） |

- `docs/concept.md` は開発者の言葉で書く文書です。AI は編集せず、読んで方向性の参考にします。
- 要件定義・仕様・設計は、それぞれのディレクトリの `README.md` を入口にします。内容が増えたら、同じディレクトリにファイルを分けます。
- 要件定義・仕様・設計には、**今作っている版のことだけ**を書きます。
- 版が完成したら `v0.1` のようなタグを付け、次の版に向けて文書を書き換えます。過去の版の文書はタグから取り出します。
- 要件には `R-001` のような ID を付けます。仕様と設計の各節は、対応する要件 ID を書いて紐づけます。要件から構想へは、構想側の ID（`F-DRAW-01` など）で紐づけます。
- アイデアを思いついたら [docs/ideas/](docs/ideas/) に置きます。構想に採用したら vision.md に反映します。

## トラブル事例の記録

- 作業中にトラブル（エラー、想定外の挙動、手戻り、判断ミス、環境の問題など）が起きたら、**小さなものでもこまめに** [docs/troubles/](docs/troubles/) に事例として残してください。
- 解決していなくても記録して構いません。未解決なら「未解決」と書きます。
- 書き方は [docs/troubles/README.md](docs/troubles/README.md) を参照してください。
- 目的は責任追及ではなく、同じことを繰り返さないための学びの共有です。
